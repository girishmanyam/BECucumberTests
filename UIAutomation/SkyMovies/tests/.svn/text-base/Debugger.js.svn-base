#import "../project.js"



settings = new MOVIES.SettingsView();
settings.closeSettingsDialog(); 

/*
var home = new MOVIES.HomeView()
 home.getReviewByMovieId("8ad58981392a9e29013934c407e37bac"_
home.populateTestData()
UIALogger.logMessage("" + home.getMovieIdsByCategoryId("1"))  */
 
 
 /*
 try {
 var target = UIATarget.localTarget();
	var host = target.host()
	var result = host.performTaskWithPathArgumentsTimeout("/usr/bin/skm_uia_download", ["data", "movies", 1], 60);
	//IOS.log_info("Response" + result.stdout);	
	var response = eval("(" + result.stdout + ")");
	 UIALogger.logMessage("" + response)
		UIALogger.logMessage("" +  response[0]["movies"]) }
catch(err)
{
	UIALogger.logMessage("" + "error in the call ")
	
	} 
*/

/*
var target = UIATarget.localTarget();
		var host = target.host()
		var result = host.performTaskWithPathArgumentsTimeout("/usr/bin/skm_uia_download", ["fetchairingsformovie", "8ad58981392a9e29013934c407e37bac"], 60);
		var result = eval("(" + result.stdout + ")");	
		UIALogger.logMessage("" + result);	

*/

/*
var home = new MOVIES.HomeView()
UIALogger.logMessage("" + home.getReviewByMovieId("8ad58981392a9e29013934c407e37bac"))  */


































/*IOS.settings.logLevel = IOS.logLevels.LOG_INFO
cardView = new MOVIES.CardView()
function fetchAiringsForMovieId(movieId) 
{
var target = UIATarget.localTarget();
var host = target.host()
var result = host.performTaskWithPathArgumentsTimeout("/usr/bin/skm_uia_download", ["fetchairingsformovie", movieId], 60);
var result = eval("(" + result.stdout + ")");	
return result;	
}	



var response = fetchAiringsForMovieId("8a3e88992acd3bcc012ad7dc2a6b3bef");

//UIALogger.logMessage("result" + expectedResponse[0]["dog"]["first"])



/* UIALogger.logMessage("channelName" + response[0]["nextOn"]["channelName"]);
UIALogger.logMessage("startDateTime" + response[0]["nextOn"]["startDateTime"]);

for(var i = 0 ; i < response[0]["TODAY"].length ;i++) 
{
UIALogger.logMessage("channelName" + response[0]["TODAY"][i]["channelName"]);
UIALogger.logMessage("startDateTime" + response[0]["TODAY"][i]["startDateTime"]); 
	} */


/*
var days =["TODAY","MON","TUE","WED","THU","FRI","SAT","SUN"];

UIALogger.logMessage("Days" + days.length);
var new_days = new Array();
var m = 0;	
for(var k =0 ; k < days.length; k++)
{
	UIALogger.logMessage("Keys" + days[k] + "is present" + cardView.isKeyPresent(response[0][days[k]]));
	
	if(cardView.isKeyPresent(response[0][days[k]]))
	  {							
		new_days[m] = days[k];
		UIALogger.logMessage("array value" + new_days[m] + "position is " + m)  
		 m = m + 1;
	  }							  
}
								  
var index = 0	
var activeDays = new Array();							  
for(var j =0 ; j < new_days.length; j++ )
{ 
	
for(var i = 0 ; i < response[0][new_days[j]].length ;i++) 
{
UIALogger.logMessage("channelName ==== " + new_days[j]+ "   " + response[0][new_days[j]][i]["channelName"]);
UIALogger.logMessage("startDateTime" + response[0][new_days[j]][i]["startDateTime"]); 
activeDays[index]	= new_days[j];
index++	;
break;

}
	
}  


UIALogger.logMessage("activeDays" + activeDays);  */

/*
 var target = UIATarget.localTarget();

var cellvalue = "SKY PREMIERE HD, 8:00PM, SKY PREMIERE";

var cellsValue = cellvalue.split(",");

if (cellsValue.length == 3 ) {

var splitCellValue1 = cellsValue[2] +" , "  + cellsValue[1] 
var splitCellValue2 = cellsValue[0] +" , " + cellsValue[1]

UIALogger.logMessage("splitCellValue1  ====== >    " + splitCellValue1);

UIALogger.logMessage("splitCellValue2  ====== >    " + splitCellValue2);  } else 
{
	var splitCellValue3 = cellsValue[1] +" , " + cellsValue[0]
	UIALogger.logMessage("splitCellValue2  ====== >    " + splitCellValue3);
	
	}  */








